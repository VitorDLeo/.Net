using System;
using System.Globalization;

namespace URI_ex_1038
{
    class Program
    {
        static void Main(string[] args)
        {
            int cod, qtd;
            double total;
            string[] vet;

            vet = Console.ReadLine().Split(' ');
            cod = int.Parse(vet[0]);
            qtd = int.Parse(vet[1]);

            switch (cod)
            {

            }
            
        }
    }
}
